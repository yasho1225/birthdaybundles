# Branding Assets

Place logo files and brand reference materials here (SVG, PNG, PDF from brand guide).

The site uses an inline SVG logo in `src/components/ui/Logo.tsx`. Replace or reference files here when official logo assets are available.
